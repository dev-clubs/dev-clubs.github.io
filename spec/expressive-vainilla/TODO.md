# todo

## now

- browser-review `Expressive Vainilla/IDE` — Run button executes JS, preview renders `drawName()` + `bounceBubbles()`
- browser-review `Expressive Vainilla/Dev Experience / Article Editor` — parse view updates live on editor change

## next

- add multi-lesson navigation to IDE story (Back / Next buttons drive lesson data + reset editor)
- port the homepage Storybook implementation into `src/pages/index.astro` if design-system parity is desired
- optimize or replace the heavy reference images used in expressive Storybook stories
- add visual regression snapshots for IDE and DevExperience stories
- add vanilla HTML component snippets outside Storybook if the design system will be consumed without Storybook

## later

- add dedicated docs pages for token consumption patterns
- consider IDE dark-mode theming for the preview iframe (currently hardcoded white background)

## blocked

- source folder `libraries/material_tokens/` is missing from the workspace
